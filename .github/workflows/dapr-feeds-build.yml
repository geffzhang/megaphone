name: DAPR Feeds Build

on:
  push:
    paths:
      - 'dapr/src/Feeds/**'
  pull_request:
    paths:
      - 'dapr/src/Feeds/**'
      
jobs:
  build:
    name: Feeds - Build
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1.7.2
      with:
        dotnet-version: 3.1.101
        source-url: https://nuget.pkg.github.com/AlexandreBrisebois/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
    - name: Install dependencies
      run: dotnet restore ./dapr/src/Feeds/Feeds.sln
    - name: Build
      run: dotnet build ./dapr/src/Feeds/Feeds.sln --configuration Release --no-restore